# Database Configuration (Required)
DATABASE_URL="postgresql://username:password@localhost:5432/freenomad_dev"  # Required: PostgreSQL connection string

# Authentication (Required for production)
NEXTAUTH_SECRET="your-nextauth-secret-key-here"           # Required: Random string for JWT signing (generate with: openssl rand -base64 32)
NEXTAUTH_URL="https://your-domain.vercel.app"             # Required: Base URL for NextAuth (auto-detected in Vercel if not set)

# External APIs (Optional)
UNSPLASH_ACCESS_KEY="your-unsplash-access-key"        # Optional: Unsplash API for city images
UNSPLASH_SECRET_KEY="your-unsplash-secret-key"        # Optional: Unsplash API secret

# Admin Configuration (Optional)
ADMIN_EMAIL="admin@freenomad.com"                     # Optional: Admin user email
