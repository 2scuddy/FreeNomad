# Database Configuration (Required)
# Neon PostgreSQL Database - Development Environment
DATABASE_URL="postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/freenomad_dev?sslmode=require"  # Required: Neon PostgreSQL connection string
# Alternative local development database (uncomment if using local PostgreSQL)
# DATABASE_URL="postgresql://username:password@localhost:5432/freenomad_dev"

# Neon Database Configuration (Optional - for advanced features)
NEON_DATABASE_URL="postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/freenomad_dev?sslmode=require"  # Neon connection string
NEON_PROJECT_ID="your-neon-project-id"                    # Optional: Neon project ID for API operations
NEON_API_KEY="your-neon-api-key"                          # Optional: Neon API key for programmatic access

# Authentication (Required for production)
NEXTAUTH_SECRET="your-nextauth-secret-key-here"           # Required: Random string for JWT signing (generate with: openssl rand -base64 32)
NEXTAUTH_URL="https://your-domain.vercel.app"             # Required: Base URL for NextAuth (auto-detected in Vercel if not set)

# External APIs (Optional)
UNSPLASH_ACCESS_KEY="your-unsplash-access-key"        # Optional: Unsplash API for city images
UNSPLASH_SECRET_KEY="your-unsplash-secret-key"        # Optional: Unsplash API secret

# Admin Configuration (Optional)
ADMIN_EMAIL="admin@freenomad.com"                     # Optional: Admin user email
